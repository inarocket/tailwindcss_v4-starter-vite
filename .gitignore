#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# Standard .gitignore for a Vite + Node.js project
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

# Dependencies ###############################################################
# Lockfiles should generally be committed, but the huge dependencies folder shouldn't.
/node_modules

# Build Artifacts ############################################################
# Output directory for the production build generated by 'npm run build'.
/dist
/dist-ssr # Optional: If using SSR features later

# Caches & Temporary Files ###################################################
# Vite internal cache directory (improves startup time).
/.vite
# Optional: ESLint cache file (if ESLint is added later).
.eslintcache
# Optional: TypeScript build cache (if using TS).
# *.tsbuildinfo

# Logs #######################################################################
# Standard log files from package managers.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local Environment Variables ################################################
# Never commit environment variables containing secrets (API keys, passwords, etc.).
# It's common practice to commit a '.env.example' file instead.
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
.env.test.local

# Operating System Files #####################################################
# Files generated by macOS, Windows, and Linux file explorers/systems.
.DS_Store
.DS_Store?
._*
.Spotlight-V700
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor Configuration #################################################
# Ignore configuration files and directories specific to certain editors.
# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains IDEs (WebStorm, IntelliJ, etc.)
.idea/

# Vim
*.sw[op] # Swap files
*~ # Backup files

# Sublime Text
*.sublime-project
*.sublime-workspace

# Other
# NetBeans project files
/nbproject/

#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# End of .gitignore
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
